# MCP server framework — OFFICIAL SDK.
# Package name is "mcp"; the CLI extra ships "mcp dev" and the Inspector.
# NOT the community package "fastmcp" (2.x) — same name, different project.
mcp[cli]>=1.2.0

# HTTP client for the NerdOS REST API
httpx>=0.27.0

# Load the miner IP from .env
python-dotenv>=1.0.0
